<div>
  <div class="row">
    <h3 class="col"><%= @brewer.name %> </h3>
    <h3 class="col text-right"> <%= @brewer.country.name %></h3>
  </div>
    <div class="row">
      <%= render partial: "/beers/beer", collection: @brewer.beer, locals: {show_details: false, beer_link: true} %>
    </div>
</div>